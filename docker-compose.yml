services:
  diagnostics-dev:
    build:
      context: .
      dockerfile: tools/docker/Dockerfile
    image: generic-diagnostics-dev:local
    working_dir: /workspace
    volumes:
      - .:/workspace
    command: >
      sh -lc "./build.py test --preset container-debug"
